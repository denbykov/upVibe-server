{
  "verbose": true,
  "ignore": ["node_modules", "plugins/dist", "plugins/src/**/*.spec.ts"],
  "watch": ["plugins/src/**/*.ts"],
  "execMap": {
    "ts": "npx tsc -p tsconfig.json && npx tsc-alias -p tsconfig.json && node --inspect dist/test/index.js"
  }
}
