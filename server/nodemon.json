{
  "verbose": true,
  "ignore": ["node_modules", "dist", "src/**/*.spec.ts"],
  "watch": ["src/**/*.ts"],
  "execMap": {
    "ts": "tsc -p tsconfig.dev.json && tsc-alias -p tsconfig.dev.json && node --trace-warnings --inspect=0.0.0.0:9229 build-debug/server.js"
  }
}
