{
  "verbose": true,
  "ignore": ["node_modules", "dist", "src/**/*.spec.ts"],
  "watch": ["src/**/*.ts"],
  "execMap": {
    "ts": "npx tsc -p tsconfig.dev.json && npx tsc-alias -p tsconfig.dev.json && node --inspect buildDebug/server.js"
  }
}
